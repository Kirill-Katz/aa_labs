#pragma once

#include <vector>

using WeightedGraph = std::vector<std::vector<std::pair<long long, long long>>>;
using Graph = std::vector<std::vector<int>>;
using Edge = std::tuple<long long,long long,long long>;
